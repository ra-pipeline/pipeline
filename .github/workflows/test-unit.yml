name: "Test-Unit"

on:
  push:
    branches:
      - "main"
    paths:
      - "docs/**"
      - "pipeline/**"
      - "tests/**"
      - ".github/workflows/**"
      - "environment*.yml"
      - "pyproject.toml"
      - "setup.py"
  pull_request:
    branches:
      - "**"
  # Allow manual triggering
  workflow_dispatch:

# Cancel in-progress runs for the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        conda_env: [environment.yml, environment_vlass2026.yml]

    name: Unit Testing (${{ matrix.os }}, ${{ matrix.conda_env }})

    uses: ./.github/workflows/env-setup-run-template.yml
    with:
      os: ${{ matrix.os }}
      conda_env: ${{ matrix.conda_env }}
      pip_install: .[dev]
      test_data: unit
      run_command: |
        # Run tests with automatic CPU detection (pyproject.toml handles coverage generation)
        # Current standard GitHub Runner specs (as of Dec 2025):
        #   ubuntu-latest: 4 CPU cores, 16 GB RAM
        #   macos-latest:  3 CPU cores,  7 GB RAM
        PYTHONNOUSERSITE=1 python -m pytest -n logical \
          --cov --cov=pipeline --cov-append \
          --cov-report=html --cov-report=xml  \
          pipeline/

      # Only upload artifacts for a single reference configuration:
      #   - ubuntu-latest OS
      #   - environment.yml conda environment
      #
      # The other matrix combinations (macOS and the environment_vlass2026.yml)
      # run tests for additional coverage but do not publish artifacts. The
      # downstream jobs (upload_pages, upload_codecov) are wired to download
      # this specific artifact name: "pytest-results-ubuntu-latest".
      artifact_name: ${{ ((matrix.os == 'ubuntu-latest' && matrix.conda_env == 'environment.yml') && 'pytest-results-ubuntu-latest') || '' }}
      artifact_path: |
        coverage.xml
        htmlcov/
        junit.xml

  upload_pages:
    name: Upload Coverage to Pages
    needs: test
    runs-on: ubuntu-latest

    if: |
      needs.test.result == 'success' &&
      github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          name: pytest-results-ubuntu-latest

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: htmlcov
          destination_dir: coverage_unit

  upload_codecov:
    name: Upload Coverage to CodeCov
    needs: test
    runs-on: ubuntu-latest
    if: needs.test.result == 'success'
    steps:
      - name: Checkout Full History for Codecov
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          name: pytest-results-ubuntu-latest

      - name: Upload coverage to CodeCov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: unittests
          name: codecov-ubuntu-latest
          fail_ci_if_error: true
          verbose: true
          disable_telem: true

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
